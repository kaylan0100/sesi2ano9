#include <stdio.h>

int main() {
    int n1, n2, n3, soma;

    scanf("%d", &n1);
    scanf("%d", &n2);
    scanf("%d", &n3);

    soma = n1 + n2 + n3;

    printf("Soma: %d", soma);

    return 0;
}
